version: '3.8'

services:
  cinemabro:
    build: .
    container_name: cinemabro
    restart: unless-stopped
    env_file: .env
    environment:
      NODE_ENV: production
    volumes:
      - ./data:/app/data
